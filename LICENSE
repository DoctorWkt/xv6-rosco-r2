This project is a conglomeration of code from
various places.

Most of the kernel code is `xv6` code and uses
the `xv6` license. There is some `rosco` code
in the kernel, too.

The libraries, include files and commands come
from Minix and under the Minix license.

There are some files which come from FreeBSD,
and they are BSD licensed.
